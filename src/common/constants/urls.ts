const URLS = {
	PROJECTS: {
		base: 'api/projects',
		createProject: '/',
		listProjects: '/',
		getProject: '/:uuid/',
		updateProject: '/:uuid/',
		deleteProject: '/:uuid/',
	},
};

export default URLS;
